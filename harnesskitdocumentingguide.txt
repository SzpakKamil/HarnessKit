================================================================================
HARNESSKIT DOCUMENTATION GUIDE
================================================================================
Purpose : Reference for writing DocC
Source  : Sources/HarnessKit/ and Sources/HarnessKitTesting/
Context : README.md — "A structured navigation harness for SwiftUI demo apps and
          component previews. Define a tree of views using PathProject and
          PathFolder, render it with HarnessView, then navigate to any screen
          in UI tests with a single call."
================================================================================


════════════════════════════════════════════════════════════════════════════════
MODULE: HarnessKit
════════════════════════════════════════════════════════════════════════════════


────────────────────────────────────────────────────────────────────────────────
FILE: Sources/HarnessKit/PathProject.swift
────────────────────────────────────────────────────────────────────────────────

LINE 1  │ import Foundation
        │ Standard library import. No DocC needed.

LINE 3  │ @MainActor
        │ All protocol requirements and extensions run on the main actor.
        │ Add a DocC note on the type: "All members must be accessed on the
        │ main actor."

LINE 4  │ public protocol PathProject: Sendable {
        │
        │ SYMBOL   : PathProject
        │ KIND     : Protocol
        │ SUMMARY  : The root of a HarnessKit view hierarchy.
        │ DISCUSSION:
        │   Conform a type (typically a caseless enum) to PathProject to
        │   define the top-level entry point for your harness app. PathProject
        │   groups one or more PathFolder sections and provides the name shown
        │   at the navigation root.
        │
        │   Example:
        │     enum MyProject: PathProject {
        │         static let name = "My App"
        │         static let folders: [any PathFolder.Type] = [ButtonsFolder.self]
        │     }
        │
        │ NOTE: Conforms to Sendable; all static properties must be safe to
        │   access from the main actor.

LINE 5  │     static var name: String { get }
        │
        │ SYMBOL   : PathProject.name
        │ KIND     : Static property (required)
        │ SUMMARY  : The display name shown as the navigation title of the root list.
        │ DISCUSSION:
        │   Provide a short, human-readable string. HarnessView uses this value
        │   as the navigationTitle of the top-level List.

LINE 6  │     static var pathIds: [Int] { get }
        │
        │ SYMBOL   : PathProject.pathIds
        │ KIND     : Static property (required)
        │ SUMMARY  : The ordered sequence of integer indices that uniquely identifies
        │            this project in the harness path.
        │ DISCUSSION:
        │   For the root project this is always an empty array []. PathFolder
        │   extensions append their own index to this array to build a complete
        │   path from root to leaf. Used internally by PathResolver and
        │   PathFolderNavigation for tvOS remote navigation.
        │ DEFAULT  : [] (provided by the extension default)

LINE 7  │     static var nameComponents: [String] { get }
        │
        │ SYMBOL   : PathProject.nameComponents
        │ KIND     : Static property (required)
        │ SUMMARY  : The ordered list of display names from the project root down
        │            to the current level.
        │ DISCUSSION:
        │   For PathProject this is always empty because the root has no parent.
        │   Each PathFolder appends its own name to the array it inherits from
        │   its parent. PathFolderNavigation uses this array to tap through each
        │   navigation level during UI tests.
        │ DEFAULT  : [] (provided by the extension default)

LINE 8  │     static var namePath: String { get }
        │
        │ SYMBOL   : PathProject.namePath
        │ KIND     : Static property (required)
        │ SUMMARY  : A slash-separated string representation of the full name path
        │            from root to the current level.
        │ DISCUSSION:
        │   Built by joining nameComponents with "/". For PathProject this is
        │   an empty string. For a nested folder it looks like "Buttons/Primary".
        │   Useful for debugging and accessibility identifiers.
        │ DEFAULT  : nameComponents.joined(separator: "/")

LINE 9  │     static var folders: [any PathFolder.Type] { get }
        │
        │ SYMBOL   : PathProject.folders
        │ KIND     : Static property (required)
        │ SUMMARY  : The ordered list of top-level folder types that belong to
        │            this project.
        │ DISCUSSION:
        │   Each element is a metatype conforming to PathFolder. HarnessView
        │   iterates this array to build the root navigation list. The order of
        │   elements determines the display order in the UI.
        │ DEFAULT  : [] — override to add folders.

LINE 12 │ extension PathProject {
        │ Base extension providing default implementations for all computed
        │ properties. Users only need to implement `name` and `folders`.

LINE 13-14 │ @_documentation(visibility: internal)
           │ public static var folders: [any PathFolder.Type] { [] }
           │
           │ SYMBOL   : PathProject.folders (default)
           │ DocC visibility is intentionally set to internal so this default
           │ does not appear in generated documentation. The requirement above
           │ documents the user-facing contract.
           │ LINE DETAIL: Returns an empty array. Projects with no folders render
           │   an empty root list.

LINE 15-16 │ @_documentation(visibility: internal)
           │ public static var pathIds: [Int] { [] }
           │
           │ SYMBOL   : PathProject.pathIds (default)
           │ LINE DETAIL: Empty array — the project root has no parent path segment.

LINE 17-18 │ @_documentation(visibility: internal)
           │ public static var nameComponents: [String] { [] }
           │
           │ SYMBOL   : PathProject.nameComponents (default)
           │ LINE DETAIL: Empty array — the project root contributes no name component.

LINE 19-20 │ @_documentation(visibility: internal)
           │ public static var namePath: String { nameComponents.joined(separator: "/") }
           │
           │ SYMBOL   : PathProject.namePath (default)
           │ LINE DETAIL: Joins nameComponents (which is []) producing "". For nested
           │   PathFolder conformances this produces the full slash path.


────────────────────────────────────────────────────────────────────────────────
FILE: Sources/HarnessKit/PathFolder.swift
────────────────────────────────────────────────────────────────────────────────

LINE 1  │ import Foundation
LINE 2  │ import SwiftUI
        │ SwiftUI is needed for the Content: View associated type and @ViewBuilder.

LINE 4  │ @MainActor
LINE 5  │ public protocol PathFolder: CaseIterable, Sendable {
        │
        │ SYMBOL   : PathFolder
        │ KIND     : Protocol
        │ SUMMARY  : A section or leaf level in a HarnessKit navigation hierarchy.
        │ DISCUSSION:
        │   Conform an enum to PathFolder to define a group of navigable views.
        │   Each enum case represents one navigable option (a leaf view). Folders
        │   can also contain sub-folders by populating `folders`.
        │
        │   PathFolder requires CaseIterable so the framework can enumerate all
        │   options automatically. Conforming types are typically Int-raw-value
        │   enums to support tvOS remote navigation.
        │
        │   Example:
        │     enum ButtonsFolder: PathFolder {
        │         typealias ParentSection = MyProject
        │         static let name = "Buttons"
        │         case primary, secondary, destructive
        │
        │         var description: String { ... }
        │
        │         @ViewBuilder
        │         var view: some View { ... }
        │     }
        │
        │ NOTE: Conforms to Sendable; all members run on the main actor.
        │   The CaseIterable conformance drives the default `options` array.

LINE 6  │     associatedtype ParentSection
        │
        │ SYMBOL   : PathFolder.ParentSection
        │ KIND     : Associated type
        │ SUMMARY  : The type that contains this folder — either a PathProject or
        │            another PathFolder.
        │ DISCUSSION:
        │   Set via typealias in every conforming type. The conditional extensions
        │   below key off this associated type to select the correct path-building
        │   logic (project root vs. nested folder).

LINE 7  │     static var name: String { get }
        │
        │ SYMBOL   : PathFolder.name
        │ KIND     : Static property (required)
        │ SUMMARY  : The display name used as the navigation title for this folder's list.

LINE 8  │     static var pathIds: [Int] { get }
        │
        │ SYMBOL   : PathFolder.pathIds (static)
        │ KIND     : Static property (required)
        │ SUMMARY  : The ordered integer path from the project root to this folder type.
        │ DISCUSSION:
        │   Each element is the index of the folder within its parent's `folders`
        │   array. Used by tvOS navigation to count remote-down presses needed to
        │   reach each level.

LINE 9  │     static var nameComponents: [String] { get }
        │
        │ SYMBOL   : PathFolder.nameComponents (static)
        │ KIND     : Static property (required)
        │ SUMMARY  : The ordered display-name components from the project root to
        │            this folder type.

LINE 10 │     static var namePath: String { get }
        │
        │ SYMBOL   : PathFolder.namePath (static)
        │ KIND     : Static property (required)
        │ SUMMARY  : A slash-joined string of all name components down to this folder type.

LINE 11 │     static var folders: [any PathFolder.Type] { get }
        │
        │ SYMBOL   : PathFolder.folders
        │ KIND     : Static property (required)
        │ SUMMARY  : The ordered list of sub-folder types nested inside this folder.
        │ DISCUSSION:
        │   HarnessView renders each element as a NavigationLink row before the
        │   option rows. Set to [] for leaf folders that contain only cases.
        │ DEFAULT  : [] (provided by the base extension)

LINE 12 │     static var options: [any PathFolder] { get }
        │
        │ SYMBOL   : PathFolder.options
        │ KIND     : Static property (required)
        │ SUMMARY  : All navigable case instances of this folder, in declaration order.
        │ DISCUSSION:
        │   Defaults to Array(allCases) via CaseIterable. Override to provide a
        │   custom ordered subset.
        │ DEFAULT  : Array(allCases)

LINE 13 │     associatedtype Content: View
        │
        │ SYMBOL   : PathFolder.Content
        │ KIND     : Associated type
        │ SUMMARY  : The SwiftUI view type returned by the `view` property.
        │ DISCUSSION:
        │   The compiler infers this type from the concrete return type of `view`
        │   at each conformance site. No explicit typealias is needed when using
        │   `some View` as the return type.

LINE 14 │     var description: String { get }
        │
        │ SYMBOL   : PathFolder.description
        │ KIND     : Instance property (required)
        │ SUMMARY  : The label displayed in the navigation list row for this option.
        │ DISCUSSION:
        │   Used by HarnessView as the title of each NavigationLink that leads
        │   to the option's view. Typically returns a human-readable string that
        │   matches the accessibility identifier of the destination view.
        │ DEFAULT  : "" (empty string — the row will still appear but with no label)

LINE 15 │     @ViewBuilder var view: Content { get }
        │
        │ SYMBOL   : PathFolder.view
        │ KIND     : Instance property (required)
        │ SUMMARY  : The SwiftUI view presented when the user navigates to this option.
        │ DISCUSSION:
        │   Implement this property to return the preview or component view
        │   associated with this folder case. The @ViewBuilder attribute lets you
        │   use if/switch branching without explicit return statements.
        │
        │   Example (single type):
        │     var view: some View {
        │         MyComponentPreview()
        │     }
        │
        │   Example (heterogeneous switch):
        │     @ViewBuilder
        │     var view: some View {
        │         switch self {
        │         case .primary:   PrimaryButtonPreview()
        │         case .secondary: SecondaryButtonPreview()
        │         }
        │     }
        │
        │ DEFAULT  : EmptyView() — renders a blank navigation destination.
        │
        │ NOTE: AnyView is never required. The library type-erases internally
        │   via the private makeView(_:) helper in HarnessView.swift.

─ Extension: base defaults ─────────────────────────────────────────────────────

LINE 18 │ extension PathFolder {
        │ Provides sensible defaults so conformers only need to implement
        │ `name`, `ParentSection`, and (optionally) `view`, `description`,
        │ and `folders`.

LINE 19-20 │ public static var folders: [any PathFolder.Type] { [] }
           │ LINE DETAIL: Default — leaf folders declare no sub-folders.

LINE 21-22 │ public static var options: [any PathFolder] { Array(allCases) }
           │ LINE DETAIL: Converts the CaseIterable allCases sequence to a plain
           │   array so it can be stored as [any PathFolder].

LINE 23-24 │ public var description: String { "" }
           │ LINE DETAIL: Empty string default. Override to provide meaningful labels.

LINE 25-26 │ public var view: some View { EmptyView() }
           │ LINE DETAIL: Returns EmptyView so cases without a view still compile.
           │   EmptyView renders as a blank screen — identical visual result to the
           │   former `nil` optional AnyView default.

─ Extension: ParentSection == PathProject ──────────────────────────────────────

LINE 29 │ extension PathFolder where ParentSection: PathProject {
        │ Activated when this folder sits directly under a PathProject root.
        │ Provides path-building implementations based on the project's arrays.

LINE 30-31 │ internal static var path: Int {
           │     ParentSection.folders.firstIndex(where: {
           │         ObjectIdentifier($0) == ObjectIdentifier(Self.self)
           │     }) ?? 0
           │ }
           │
           │ SYMBOL   : path (internal)
           │ KIND     : Static computed property
           │ SUMMARY  : The zero-based index of this folder within its parent's folders array.
           │ LINE DETAIL (ObjectIdentifier):
           │   `firstIndex(where:)` iterates `ParentSection.folders` which holds
           │   `any PathFolder.Type` (metatype existentials). Two metatype
           │   existentials are compared by wrapping them in ObjectIdentifier —
           │   this produces a stable identity token from the underlying class/type
           │   pointer, enabling correct equality even through the existential wrapper.
           │   Falls back to 0 if the folder is not found (should not happen in a
           │   correctly configured hierarchy).

LINE 33-34 │ public static var pathIds: [Int] { ParentSection.pathIds + [path] }
           │ LINE DETAIL: Prepends the parent project's pathIds (always []) with
           │   this folder's own index, producing e.g. [1] for the second folder.

LINE 35-36 │ public static var nameComponents: [String] { ParentSection.nameComponents + [name] }
           │ LINE DETAIL: Appends this folder's name to the parent's name array.

LINE 37-38 │ public static var namePath: String { nameComponents.joined(separator: "/") }
           │ LINE DETAIL: E.g. "Buttons" for a top-level folder.

─ Extension: ParentSection == PathFolder ───────────────────────────────────────

LINE 41 │ extension PathFolder where ParentSection: PathFolder {
        │ Activated when this folder is nested inside another PathFolder.
        │ Identical logic to the PathProject extension but walks the folder tree
        │ instead of the project root.

LINE 42-44 │ internal static var path: Int { ... }
           │ LINE DETAIL: Same ObjectIdentifier comparison as above, but now
           │   searching within the parent *folder's* folders array. Correctly
           │   handles arbitrarily deep nesting.

LINE 45-50 │ pathIds / nameComponents / namePath
           │ LINE DETAIL: Each appends to ParentSection's arrays, so the final
           │   values contain the complete path from the project root.

─ Extension: RawRepresentable where RawValue == Int ────────────────────────────

LINE 53 │ extension PathFolder where Self: RawRepresentable, RawValue == Int {
        │ Activated for Int-raw-value enums. Provides instance-level path
        │ properties that include the specific case's raw value, enabling
        │ tvOS navigation to count the exact number of remote presses needed.

LINE 54-55 │ public var pathIds: [Int] { Self.pathIds + [self.rawValue] }
           │
           │ SYMBOL   : PathFolder.pathIds (instance)
           │ KIND     : Instance property
           │ SUMMARY  : The complete ordered integer path from the project root to
           │            this specific case.
           │ LINE DETAIL: Appends the case's own rawValue to the type-level pathIds.
           │   For example, if Self.pathIds == [1] and self == .secondary (rawValue 1),
           │   the result is [1, 1].

LINE 57-58 │ private var caseName: String {
           │     Mirror(reflecting: self).children.first?.label ?? String(describing: self)
           │ }
           │
           │ SYMBOL   : caseName (private)
           │ KIND     : Instance computed property
           │ SUMMARY  : The Swift identifier name of the enum case as a string.
           │ LINE DETAIL (Mirror):
           │   For enums with associated values, Mirror provides a children
           │   sequence where the first child's label is the case name string.
           │   For simple enums without associated values, Mirror's children is
           │   empty, so the fallback String(describing: self) is used — which
           │   returns the case name directly (e.g. "primary").
           │   This avoids hardcoding case names and works correctly for any
           │   CaseIterable Int-raw-value enum.

LINE 61-62 │ public var nameComponents: [String] { Self.nameComponents + [caseName] }
           │
           │ SYMBOL   : PathFolder.nameComponents (instance)
           │ SUMMARY  : The complete ordered name path from root to this specific case.
           │ LINE DETAIL: Appends the case's own display name to the type-level array,
           │   producing e.g. ["Buttons", "primary"].

LINE 63-64 │ public var namePath: String { nameComponents.joined(separator: "/") }
           │
           │ SYMBOL   : PathFolder.namePath (instance)
           │ SUMMARY  : Slash-separated full path string for this case, e.g.
           │            "Buttons/primary". Useful as an accessibility identifier.


────────────────────────────────────────────────────────────────────────────────
FILE: Sources/HarnessKit/PathResolver.swift
────────────────────────────────────────────────────────────────────────────────

LINE 1  │ import Foundation

LINE 3  │ extension PathFolder {
        │ Convenience static methods that expose path resolution through a
        │ consistent API. Two families: instance-based (for a specific case)
        │ and type-based (for a folder type as a whole).

─ Instance-based resolver methods (require RawRepresentable) ───────────────────

LINE 4-6 │ public static func ids<N: PathFolder & RawRepresentable>(
         │     for node: N
         │ ) -> [Int] where N.RawValue == Int
         │
         │ SYMBOL   : PathFolder.ids(for:)
         │ KIND     : Generic static method
         │ SUMMARY  : Returns the complete integer path for a specific folder case.
         │ DISCUSSION:
         │   Delegates to node.pathIds (the instance-level property provided by
         │   the RawRepresentable extension). The result includes the case's own
         │   rawValue as the final element.
         │ PARAMETERS:
         │   - node: A specific enum case conforming to PathFolder and
         │           RawRepresentable<Int>.
         │ RETURNS  : [Int] — ordered path indices from project root to this case.

LINE 7-9 │ public static func names<N: PathFolder & RawRepresentable>(
         │     for node: N
         │ ) -> [String] where N.RawValue == Int
         │
         │ SYMBOL   : PathFolder.names(for:)
         │ SUMMARY  : Returns the complete display-name path for a specific folder case.
         │ PARAMETERS:
         │   - node: A specific enum case.
         │ RETURNS  : [String] — e.g. ["Shapes", "BasicShapes", "circle"].
         │   Each string matches the label shown in the navigation list at that level.

LINE 10-12 │ public static func namePath<N: PathFolder & RawRepresentable>(
           │     for node: N
           │ ) -> String where N.RawValue == Int
           │
           │ SYMBOL   : PathFolder.namePath(for:)
           │ SUMMARY  : Returns the slash-joined path string for a specific folder case.
           │ PARAMETERS:
           │   - node: A specific enum case.
           │ RETURNS  : String — e.g. "Shapes/BasicShapes/circle".

─ Type-based resolver methods ──────────────────────────────────────────────────

LINE 14-16 │ public static func ids<N: PathFolder>(forType type: N.Type) -> [Int]
           │
           │ SYMBOL   : PathFolder.ids(forType:)
           │ SUMMARY  : Returns the integer path for a folder type (not a specific case).
           │ DISCUSSION:
           │   Use this when you need the path to navigate to the folder level
           │   itself, not to a specific case within it.
           │ PARAMETERS:
           │   - type: The PathFolder metatype, e.g. ButtonsFolder.self.
           │ RETURNS  : [Int] — ordered path indices from root to this folder type.

LINE 17-19 │ public static func names<N: PathFolder>(forType type: N.Type) -> [String]
           │
           │ SYMBOL   : PathFolder.names(forType:)
           │ SUMMARY  : Returns the display-name components for a folder type.
           │ PARAMETERS:
           │   - type: The PathFolder metatype.
           │ RETURNS  : [String] — e.g. ["Shapes", "BasicShapes"].

LINE 20-22 │ public static func namePath<N: PathFolder>(forType type: N.Type) -> String
           │
           │ SYMBOL   : PathFolder.namePath(forType:)
           │ SUMMARY  : Returns the slash-joined path string for a folder type.
           │ PARAMETERS:
           │   - type: The PathFolder metatype.
           │ RETURNS  : String — e.g. "Shapes/BasicShapes".


────────────────────────────────────────────────────────────────────────────────
FILE: Sources/HarnessKit/HarnessView.swift
────────────────────────────────────────────────────────────────────────────────

LINE 1-6  │ File header comment. No DocC.

LINE 8    │ import SwiftUI

─ HarnessView ──────────────────────────────────────────────────────────────────

LINE 10   │ @MainActor
LINE 11   │ public struct HarnessView<Project: PathProject>: View {
          │
          │ SYMBOL   : HarnessView
          │ KIND     : Generic SwiftUI View struct
          │ SUMMARY  : The root view of a HarnessKit harness app.
          │ DISCUSSION:
          │   Provide your PathProject conformance as the generic parameter.
          │   HarnessView builds a NavigationStack (iOS 16+/macOS 13+/tvOS 16+)
          │   or a NavigationView (older OS) whose root is a list of all top-level
          │   folders in the project.
          │
          │   Usage:
          │     struct HarnessApp: App {
          │         var body: some Scene {
          │             WindowGroup { HarnessView<MyProject>() }
          │         }
          │     }
          │
          │ GENERIC PARAMETER:
          │   - Project: A type conforming to PathProject that defines the
          │              navigation hierarchy.

LINE 12   │     public init() {}
          │
          │ SYMBOL   : HarnessView.init()
          │ SUMMARY  : Creates a HarnessView for the specified PathProject.
          │ DISCUSSION:
          │   No configuration needed — all structure is derived from the
          │   Project generic parameter.

LINE 14-24 │ public var body: some View { ... }
           │
           │ SYMBOL   : HarnessView.body
           │ SUMMARY  : The rendered view hierarchy.
           │ LINE DETAIL (availability guard):
           │   Uses if #available(iOS 16, macOS 13, tvOS 16, *) to select
           │   NavigationStack on modern OS versions (which supports programmatic
           │   navigation and avoids NavigationView deprecation warnings) and falls
           │   back to NavigationView on iOS 14/15 and macOS 11/12.
           │ NOTE: body is marked @_documentation(visibility: internal) to hide
           │   the implementation detail from DocC output.

LINE 26-37 │ @ViewBuilder
           │ private var projectList: some View { ... }
           │
           │ SYMBOL   : projectList (private)
           │ KIND     : Computed property
           │ SUMMARY  : Builds the root List from Project.folders.
           │ LINE DETAIL:
           │   ForEach(Project.folders.indices, id: \.self) — iterates by index
           │   (not by element) because `any PathFolder.Type` is not Hashable and
           │   cannot be used as a ForEach identifier directly.
           │   Each folder produces a NavigationLink whose destination is a
           │   HarnessFolderView initialized with that folder's metatype.
           │   .navigationTitle(Project.name) sets the nav bar title.

─ makeView (private file-scope helper) ─────────────────────────────────────────

LINE 40-43 │ @MainActor
           │ private func makeView(_ option: some PathFolder) -> AnyView {
           │     AnyView(option.view)
           │ }
           │
           │ SYMBOL   : makeView(_:)
           │ KIND     : Private generic free function
           │ SUMMARY  : Type-erases a PathFolder option's view to AnyView.
           │ DISCUSSION:
           │   PathFolder.view returns an associated type Content: View. When
           │   options are stored as [any PathFolder] (existentials), the
           │   compiler cannot statically determine Content at the call site
           │   inside a @ViewBuilder block. This function accepts `some PathFolder`
           │   (a generic parameter), causing Swift to implicitly open the
           │   existential and resolve Content concretely within the function body,
           │   where AnyView(option.view) is then valid.
           │   Returning AnyView provides a concrete, statically-known View type
           │   that composes cleanly inside NavigationLink's @ViewBuilder.
           │
           │   This keeps AnyView as a private implementation detail — public API
           │   and all user-facing conformances remain free of it.
           │
           │ PARAMETERS:
           │   - option: Any value conforming to PathFolder (existential opened
           │             by the `some` parameter).
           │ RETURNS  : AnyView wrapping option.view.

─ HarnessFolderView (internal) ─────────────────────────────────────────────────

LINE 45-46 │ @MainActor
           │ struct HarnessFolderView: View {
           │
           │ SYMBOL   : HarnessFolderView
           │ KIND     : Internal SwiftUI View struct
           │ SUMMARY  : Renders the list of sub-folders and navigable options for
           │            a single PathFolder level.
           │ DISCUSSION:
           │   Used recursively — selecting a sub-folder row pushes another
           │   HarnessFolderView, while selecting an option row pushes the
           │   option's view. Not part of the public API.

LINE 47   │     let folder: any PathFolder.Type
          │ LINE DETAIL: Stored as an existential metatype so that folders with
          │   different concrete types can be passed to the same struct. The
          │   concrete type is opened lazily via `some PathFolder` parameters
          │   (in makeView) where needed.

LINE 49-51 │ var body: some View { folderList }
           │ LINE DETAIL: Delegates entirely to folderList for clean separation.

LINE 53-69 │ @ViewBuilder
           │ private var folderList: some View { ... }
           │
           │ SYMBOL   : folderList (private)
           │ SUMMARY  : Builds the List containing sub-folder rows and option rows.
           │ LINE DETAIL (sub-folder section):
           │   ForEach(folder.folders.indices, id: \.self) — same index-based
           │   iteration as projectList. Each row is a NavigationLink that pushes
           │   a new HarnessFolderView(folder: subfolder).
           │ LINE DETAIL (option section):
           │   ForEach(folder.options.indices, id: \.self) — iterates the options
           │   array (which is [any PathFolder]). Each row is a NavigationLink
           │   whose title is option.description and whose destination is the
           │   result of makeView(option) — the private type-erasing helper.
           │ LINE DETAIL (.navigationTitle):
           │   Sets the nav bar title to folder.name, so each level shows its
           │   own name as the user drills down.

LINE 71-73 │ init(folder: any PathFolder.Type) {
           │     self.folder = folder
           │ }
           │ SYMBOL   : HarnessFolderView.init(folder:)
           │ SUMMARY  : Creates a HarnessFolderView for the given folder metatype.
           │ PARAMETERS:
           │   - folder: The PathFolder metatype whose sub-folders and options
           │             this view will render.


════════════════════════════════════════════════════════════════════════════════
MODULE: HarnessKitTesting
════════════════════════════════════════════════════════════════════════════════


────────────────────────────────────────────────────────────────────────────────
FILE: Sources/HarnessKitTesting/PathFolderNavigation.swift
────────────────────────────────────────────────────────────────────────────────

LINE 1  │ import XCTest
LINE 2  │ import HarnessKit

─ navigate(app:) — folder-type overload ────────────────────────────────────────

LINE 4  │ extension PathFolder {
LINE 5-6 │ @MainActor
         │ public func navigate(app: XCUIApplication) {
         │
         │ SYMBOL   : PathFolder.navigate(app:)  [folder-type overload]
         │ KIND     : Instance method
         │ SUMMARY  : Navigates to this folder's view in a running XCUIApplication.
         │ DISCUSSION:
         │   Taps or clicks through every navigation level from the root to this
         │   folder using the display names in Self.nameComponents (type-level).
         │   Use this overload when the conforming type does NOT have Int raw values
         │   (i.e., it's a folder-only type without navigable cases).
         │
         │   Example:
         │     ButtonsFolder.navigate(app: app)  // navigates to the folder list
         │
         │   Note: tvOS is not supported for this overload because without
         │   RawRepresentable, the row index (needed for remote d-pad presses)
         │   cannot be determined. Calling this on tvOS triggers a fatalError.
         │
         │ PARAMETERS:
         │   - app: The running XCUIApplication instance.

LINE 7-10 │ #if !os(macOS) && !os(tvOS)
          │ for name in Self.names(forType: Self.self) {
          │     tapButtonWithScrolling(app: app, titleOrIdentifier: name)
          │ }
          │ LINE DETAIL: On iOS/watchOS, iterates the folder's name components and
          │   taps each level's button by label, scrolling if needed.
          │   Self.names(forType: Self.self) calls the PathResolver helper to get
          │   the ordered name array for the folder type itself (not a specific case).

LINE 11-14 │ #elseif os(macOS)
           │ for name in Self.names(forType: Self.self) {
           │     clickButtonWithScrolling(app: app, titleOrIdentifier: name)
           │ }
           │ LINE DETAIL: On macOS, uses clickButtonWithScrolling instead of tap,
           │   adapting to mouse/pointer interaction.

LINE 15-17 │ #else
           │ fatalError("Not supported on tvOS types without RawRepresentable")
           │ LINE DETAIL: tvOS requires knowing the raw integer index to compute
           │   the exact number of XCUIRemote down presses. Without RawRepresentable,
           │   this information is unavailable. The RawRepresentable overload below
           │   handles the tvOS case correctly.

─ navigate(app:) — RawRepresentable overload ───────────────────────────────────

LINE 21 │ extension PathFolder where Self: RawRepresentable, RawValue == Int {
LINE 22-23 │ @MainActor
           │ public func navigate(app: XCUIApplication) {
           │
           │ SYMBOL   : PathFolder.navigate(app:)  [RawRepresentable overload]
           │ KIND     : Instance method
           │ SUMMARY  : Navigates to a specific folder case in a running XCUIApplication.
           │ DISCUSSION:
           │   Handles all platforms including tvOS. For touch/pointer platforms it
           │   taps or clicks each level by name. For tvOS it uses XCUIRemote to
           │   press directional buttons and select, navigating entirely by index.
           │
           │   Example:
           │     ButtonsFolder.destructive.navigate(app: app)
           │
           │ PARAMETERS:
           │   - app: The running XCUIApplication instance.

LINE 24-27 │ #if !os(macOS) && !os(tvOS)
           │ for name in Self.names(for: self) {
           │     tapButtonWithScrolling(app: app, titleOrIdentifier: name)
           │ }
           │ LINE DETAIL: On iOS/watchOS, uses Self.names(for: self) — the
           │   instance-level resolver — which includes the case's own name as the
           │   last element (e.g. ["Buttons", "destructive"]).

LINE 28-31 │ #elseif os(macOS) — same pattern using clickButtonWithScrolling.

LINE 32-46 │ #else  (tvOS)
           │ app.activate()
           │ LINE DETAIL: Brings the app to the foreground before any remote presses.
           │
           │ for id in Self.pathIds {
           │     for _ in 0..<id {
           │         XCUIRemote.shared.press(.down)
           │     }
           │     XCUIRemote.shared.press(.select)
           │     sleep(1)
           │ }
           │ LINE DETAIL (folder traversal):
           │   Self.pathIds contains the type-level path (e.g. [1] for the second
           │   top-level folder). For each id, the code presses .down `id` times
           │   to move the Siri Remote focus to the correct row, then presses
           │   .select to navigate into it. sleep(1) waits for the navigation
           │   animation to complete before pressing again.
           │
           │ let optionRow = Self.folders.count + self.rawValue
           │ LINE DETAIL: In tvOS list rows, sub-folder rows appear first (one per
           │   element in Self.folders), followed by option rows in rawValue order.
           │   optionRow is the zero-based index of this case's row within the
           │   final folder list.
           │
           │ for _ in 0..<optionRow {
           │     XCUIRemote.shared.press(.down)
           │ }
           │ XCUIRemote.shared.press(.select)
           │ sleep(1)
           │ LINE DETAIL: Presses .down optionRow times to move focus from the
           │   first row to the target case's row, then selects it. sleep(1)
           │   waits for the destination view to load.


────────────────────────────────────────────────────────────────────────────────
FILE: Sources/HarnessKitTesting/NavigationMethods.swift
────────────────────────────────────────────────────────────────────────────────

LINE 1  │ import XCTest

LINE 3  │ #if os(iOS) || os(watchOS)
        │ All functions in this block are compiled only on iOS and watchOS.

─ smallSwipeUp(on:distanceRatio:hold:) ─────────────────────────────────────────

LINE 4-5 │ @MainActor
         │ func smallSwipeUp(
         │     on element: XCUIElement,
         │     distanceRatio: CGFloat = 0.2,
         │     hold: TimeInterval = 0.01
         │ )
         │
         │ SYMBOL   : smallSwipeUp(on:distanceRatio:hold:)
         │ KIND     : Internal free function (iOS + watchOS)
         │ SUMMARY  : Performs a short upward swipe on a specific UI element to
         │            scroll a list.
         │ PARAMETERS:
         │   - element      : The XCUIElement to swipe (typically a table or
         │                    scroll view).
         │   - distanceRatio: Fraction of the element's height to swipe. Clamped
         │                    to [0.02, 0.9]. Default 0.2 (20%).
         │   - hold         : Press duration before beginning the drag. Default
         │                    0.01 s — short enough to register as a swipe.

LINE 6-8 │ #if os(watchOS)
         │     XCUIDevice.shared.rotateDigitalCrown(delta: 0.2, velocity: 0.24)
         │     sleep(1)
         │ LINE DETAIL: watchOS has no touchscreen swipe — the Digital Crown is
         │   the scroll mechanism. delta: 0.2 is a small rotation; velocity: 0.24
         │   keeps the scroll gentle. sleep(1) waits for the crown scroll animation.

LINE 9-15 │ #else  (iOS path)
          │ guard element.exists else { return }
          │ LINE DETAIL: Guards against calling coordinate(withNormalizedOffset:)
          │   on a non-existent element, which would throw an exception.
          │
          │ let start = element.coordinate(withNormalizedOffset: CGVector(dx: 0.5, dy: 0.7))
          │ LINE DETAIL: Anchors the swipe start at the center-bottom area of the
          │   element (70% from top). Starting lower avoids any bottom-bar overlap.
          │
          │ let dy = max(0.02, min(0.9, distanceRatio))
          │ LINE DETAIL: Clamps distanceRatio so the end coordinate never goes
          │   offscreen (< 0 or > 1 normalized).
          │
          │ let end = element.coordinate(withNormalizedOffset: CGVector(dx: 0.5, dy: 0.7 - dy))
          │ LINE DETAIL: The end point is above the start by dy, producing an
          │   upward swipe (content scrolls up, new content appears below).
          │
          │ start.press(forDuration: hold, thenDragTo: end)
          │ LINE DETAIL: XCTest's press-then-drag gesture simulates a slow swipe,
          │   more reliable than a fast flick for list scrolling in UI tests.

─ smallSwipeUp(app:distanceRatio:hold:) ────────────────────────────────────────

LINE 18-25 │ @MainActor
           │ func smallSwipeUp(
           │     app: XCUIApplication,
           │     distanceRatio: CGFloat = 0.2,
           │     hold: TimeInterval = 0.01
           │ )
           │
           │ SYMBOL   : smallSwipeUp(app:distanceRatio:hold:)
           │ KIND     : Internal free function (iOS + watchOS)
           │ SUMMARY  : Performs a short upward swipe on the best available
           │            scrollable element in the app.
           │ DISCUSSION:
           │   Selects the scroll target using a priority chain:
           │     1. app.tables.firstMatch (List/UITableView — most common in harness)
           │     2. app.collectionViews.firstMatch (grid layouts)
           │     3. app.scrollViews.firstMatch (custom scroll containers)
           │     4. app.windows.firstMatch (fallback — swipes on the window itself)
           │   Delegates to smallSwipeUp(on:distanceRatio:hold:) with the chosen target.
           │ PARAMETERS:
           │   - app          : The XCUIApplication to scroll.
           │   - distanceRatio: Swipe distance fraction. Default 0.2.
           │   - hold         : Press duration before drag. Default 0.01 s.

LINE 20-24 │ let target = app.tables.firstMatch.exists ? app.tables.firstMatch
           │            : app.collectionViews.firstMatch.exists ? ...
           │            : app.scrollViews.firstMatch.exists ? ...
           │            : app.windows.firstMatch
           │ LINE DETAIL: Ternary chain probes each container type in descending
           │   specificity. Tables (HarnessView's List) are checked first because
           │   they are the dominant scroll container in HarnessKit.

─ tapButtonWithScrolling(app:titleOrIdentifier:maxSwipes:) ─────────────────────

LINE 27-28 │ @MainActor
           │ func tapButtonWithScrolling(
           │     app: XCUIApplication,
           │     titleOrIdentifier: String,
           │     maxSwipes: Int = 40
           │ )
           │
           │ SYMBOL   : tapButtonWithScrolling(app:titleOrIdentifier:maxSwipes:)
           │ KIND     : Internal free function (iOS + watchOS)
           │ SUMMARY  : Finds a button by label or accessibility identifier and taps
           │            it, scrolling the list if the button is not yet visible.
           │ PARAMETERS:
           │   - app               : The XCUIApplication to interact with.
           │   - titleOrIdentifier : The button's label text or accessibilityIdentifier.
           │   - maxSwipes         : Maximum number of scroll attempts before failing.
           │                         Default 40.

LINE 29-33 │ var button = app.buttons[titleOrIdentifier]
           │ if button.exists && button.isHittable { button.tap(); return }
           │ LINE DETAIL: Fast path — if the button is immediately visible and
           │   hittable, tap it and return. isHittable confirms the element is not
           │   obscured by another view.
           │
           │ let predicate = NSPredicate(format: "label == %@ OR identifier == %@", ...)
           │ button = app.buttons.matching(predicate).firstMatch
           │ LINE DETAIL: Falls back to a predicate query that matches on either
           │   the human-readable label or the accessibilityIdentifier. This handles
           │   cases where the button title shown on screen differs from the
           │   identifier used in code (e.g. "Primary Button" vs "primary").

LINE 37-39 │ let scrollable = app.tables.firstMatch.exists ? ...
           │ LINE DETAIL: Same priority chain as smallSwipeUp(app:) for finding
           │   the scroll container.

LINE 41-50 │ while (!button.exists || !button.isHittable) && attempts < maxSwipes {
           │     smallSwipeUp(on:/app:, distanceRatio: 0.18)
           │     attempts += 1
           │     RunLoop.current.run(until: Date().addingTimeInterval(0.05))
           │ }
           │ LINE DETAIL: Scroll loop — swipes 0.18 of the container height per
           │   iteration (a small step to avoid scrolling past the target).
           │   RunLoop.run(until:) yields to the main run loop for 50 ms after
           │   each swipe, allowing XCTest to update element state before the
           │   next exists check.

LINE 52-69 │ Post-loop tap logic:
           │   if button.exists:
           │     if button.isHittable → tap immediately
           │     else → button found but partially obscured:
           │       Perform a reverse micro-swipe (down.press thenDragTo start)
           │       to nudge the button fully into view, then smallSwipeUp by 0.12.
           │       Re-check isHittable; if still not hittable, XCTFail.
           │   else:
           │     XCTFail — button never appeared after maxSwipes scrolls.
           │
           │ LINE DETAIL (reverse swipe):
           │   let start = coordinate(dx:0.5, dy:0.5)   ← center
           │   let down  = coordinate(dx:0.5, dy:0.65)  ← slightly below center
           │   down.press(forDuration: 0.01, thenDragTo: start)
           │   Dragging from below-center to center is a short downward scroll
           │   that can reveal the bottom edge of a partially-visible button.

─ clickButtonWithScrolling(app:titleOrIdentifier:maxScrolls:) ──────────────────

LINE 77-78 │ @MainActor
           │ func clickButtonWithScrolling(
           │     app: XCUIApplication,
           │     titleOrIdentifier: String,
           │     maxScrolls: Int = 20
           │ )
           │
           │ SYMBOL   : clickButtonWithScrolling(app:titleOrIdentifier:maxScrolls:)
           │ KIND     : Internal free function (macOS only, #if os(macOS))
           │ SUMMARY  : Finds a button by label or accessibility identifier and
           │            clicks it, scrolling the window if needed.
           │ PARAMETERS:
           │   - app               : The XCUIApplication.
           │   - titleOrIdentifier : Label text or accessibilityIdentifier.
           │   - maxScrolls        : Maximum scroll attempts. Default 20 (fewer than
           │                         iOS because macOS scroll is deterministic).

LINE 79-83 │ Fast path — same exists + isHittable check as tapButtonWithScrolling,
           │ but calls button.click() instead of button.tap().

LINE 84-85 │ NSPredicate fallback — identical to the iOS version.

LINE 87    │ let scrollView = app.scrollViews.firstMatch
           │ LINE DETAIL: macOS only has scrollViews (no tables/collectionViews in
           │   the XCTest macOS element hierarchy for SwiftUI Lists), so the
           │   priority chain collapses to a single element.

LINE 89-97 │ while loop:
           │   if scrollView.exists → scrollView.scroll(byDeltaX: 0, deltaY: -2)
           │   LINE DETAIL: deltaY: -2 scrolls content up by 2 points per iteration.
           │   Negative deltaY on macOS scrolls upward (revealing content below).
           │   else → app.typeKey(.pageDown, modifierFlags: [])
           │   LINE DETAIL: When no scroll view is detected, a Page Down keystroke
           │   is sent to the app, which typically moves focus in a list.
           │   RunLoop.run(until: 0.05s) waits for state refresh.

LINE 99-103 │ Post-loop:
            │ if button.exists && button.isHittable → click
            │ else → XCTFail (no reverse-swipe recovery on macOS since the
            │   scroll is already precise and there's no partial-visibility issue).


================================================================================
END OF DOCUMENTATION GUIDE
================================================================================
